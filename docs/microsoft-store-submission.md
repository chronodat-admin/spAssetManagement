# Microsoft Store Submission Guide — Asset Management

**Version:** 1.0.0.0

## Pre-submission checklist (Asset Management)

- [ ] Bump `version` in `config/package-solution.json` (currently **1.0.0.3**)
- [ ] Run `npm run ship`
- [ ] Run `npm run verify:version && npm run verify:display-name && npm run verify:store`

Current package version: **1.0.0.3** (update `config/package-solution.json` before each store upload).
